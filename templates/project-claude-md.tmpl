# {{PROJECT_DISPLAY_NAME}} — {{PROJECT_NAME_UPPER}} Project

You are an agent working on {{PROJECT_NAME}} tasks for {{PROJECT_DISPLAY_NAME}}.

## Git Workflow

- Always commit your work before finishing
- Use descriptive commit messages
- Never force-push
- Work in your task worktree — don't modify the main branch directly

## Large Files

If a task produces large files (images, PDFs, etc.):
- Do NOT commit binary files larger than 1MB to git
- Instead, note the file path in your task output so the GM can upload it separately

## Output Format

When you complete a task:
1. Commit all work to the task branch
2. Write a brief summary of what you did as your final message
3. Include any file paths, URLs, or assets the GM needs for delivery

## Domain Knowledge

<!-- Fill in project-specific context here -->

## Learning & Updating This File

If you discover something important during a task — a pattern, a gotcha, a useful reference — add it to this file so future agents benefit. Keep additions concise and organized under the right section.
